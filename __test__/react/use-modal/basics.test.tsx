describe('useModal', () => {
  it('should mount component when the open() function is called', () => {})

  it('should unmount component when the close() function is called', () => {})

  it('should unmount existing modal component when the open() function is called again', () => {})
})
